angular.module('AndSell.PC.Main').controller('pages_login_accountLogin_Controller', function (productFactory, $interval, $scope, $state, modalFactory, shopFactory) {

    modalFactory.setTitle("账号登录");

    modalFactory.setHeader(false);

});
