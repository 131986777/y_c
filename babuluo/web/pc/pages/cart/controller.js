angular.module('AndSell.PC.Main').controller('pages_cart_Controller', function (productFactory, $interval, $scope, $state, modalFactory, shopFactory) {

    modalFactory.setTitle("购物车");

    modalFactory.setHeader(false);

});

